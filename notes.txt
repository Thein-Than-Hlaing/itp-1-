git cmd = command
git status(finding changes)
git add filename, ., * (.=gitignore, * = all)
3 git commit -m " comment"
if new account
(i) git config --global user.name "user name"
(ii) git config -- global user. email "user email"
4. git push -u origin main
